# === Data & storage ===
PDF_PATH=data/mastersprograminanalytics.pdf
HTML_DIR=data
CHROMA_DIR=.chroma

# === Embedding & Chat models ===
EMBED_MODEL=text-embedding-3-large
CHAT_MODEL=gpt-4o-mini

# === Token-Based Chunking ===
# Use token-based chunking instead of character-level chunking
CHUNK_TOKENS=600
OVERLAP_TOKENS=150

# === Retrieval / Grounding ===
TOP_K=5
MIN_SIM=0.20  # Lowered from 0.30 for better recall with query expansion

# === Generation ===
TEMPERATURE=0.2
MAX_TOKENS=800

# === OpenAI API key ===
OPENAI_API_KEY=your-api-key-here
